from setuptools import setup, find_packages

setup(
    name="biozig",
    version="0.2.0",
    packages=find_packages(),
    description="Python bindings for the BioZig computational biology engine",
)
